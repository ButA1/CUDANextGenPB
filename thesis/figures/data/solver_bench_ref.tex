% generated by scripts/plot_solver_bench.py -- do not edit
\def\solverMetric{solve stage}
\def\solverRepeats{3}
\def\solverUnit{relative to the fastest}
\def\solverMolecules{1CCM, 6VYB, 1vsz, H1N1}
\def\solverEnergyMethod{1}
\def\solverMachine{}
\def\solverCaveats{ 1CCM: 10 runs per configuration, not 3. H1N1: measured on an H200 node of the TU Berlin HPC with 8 MPI ranks; 1 run per configuration, not 3 (hence no whiskers); \texttt{energy\_method\,=\,2} instead of 1 (read only after the solve stage timed here, so it does not affect this comparison).}
