% ------------------------------------------------------------------------
%  FMM implementation, group 8: fused L2P and P2P evaluation.
%  Include with \input{figures/fmm_g8}
%  Companion to the pipeline overview in figures/fmm_overview.tex.
% ------------------------------------------------------------------------
\input{figures/fmm_style}

\begin{figure}[!htb]
\centering
\begin{tikzpicture}[
    >={Latex[length=1.8mm]},
    g/.style={fmmgpu, text width=14.6cm},
    c/.style={fmmcpu, text width=14.6cm},
]
\node[c, anchor=north west] (v0) at (0,0) {%
  \fmmH{\texttt{launch\_l2p\_p2p<FIELD>()}}
  \fmmK{switch on $p$}{--- runtime order $\rightarrow$ compile-time \texttt{P}; \texttt{FIELD} selects $\Phi$ or $\nabla\Phi$}};

\node[g, anchor=north west] (v1) at ([yshift=-3mm]v0.south west) {%
  \fmmH{\texttt{l2p\_p2p\_kernel<FIELD,P>}}
  \fmmK{1 block / target \textbf{box leaf}, 1 thread / target point}{--- grid $=$ \texttt{n\_leaves}, 256 threads / block}
  \fmmK{fmm\_drain\_p2p<FIELD>}{--- near field, over this leaf's CSR row}
  \fmmK{l2p\_contract<P,T>}{--- far field; \texttt{T=dual} gives $\nabla\Phi$ by forward-mode AD}
  \fmmK{scatter through d\_orig}{--- back into the caller's point order}};

\node[c, anchor=north west] (v2) at ([yshift=-3mm]v1.south west) {%
  \fmmH{\texttt{reduce\_sum\_host()}}
  \fmmK{D2H copy, sequential host sum}{--- in caller order, matching the CPU baseline}};

\foreach \n in {v1}
  \node[fmmtag] at ([shift={(-1.4mm,-1.2mm)}]\n.north east) {GPU};
\foreach \n in {v0,v2}
  \node[fmmtag] at ([shift={(-1.4mm,-1.2mm)}]\n.north east) {CPU};
\foreach \a/\b in {v0/v1, v1/v2}
  \draw[fmmflow] (\a.south) -- (\b.north);

\end{tikzpicture}
\caption[Group 8: fused L2P and P2P evaluation]{\textbf{Group~8: fused L2P and P2P evaluation.} Solid boxes are
    GPU kernels, dashed boxes host steps; italics give the parallel mapping.}
\label{fig:fmm-g8}
\end{figure}
