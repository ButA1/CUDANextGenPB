\section{Results}
\label{sec:evaluation-results}

In this section I first touch on the improvements to the host-side optimizations to establish the speedup, which both the baseline and the CUDA optimization benefit from.
Next I will show the comparison between LIS and AMGX, as well as the tuned configuration for the latter, on both the local machine and the cluster.
The energy calculation results are a lot more intricate, particularly due to my FMM implementation having different parameters.
It also lead to the discovery, that due to NGPB evaluating the molecular atoms against the surface, having a shared leaf size for both trees was actually detrimental.

\subsection{Create Markers}
\label{sec:evaluation-results-create-markers}

\input{figures/create_markers_speedup}

For 6VYB, \snakecase{create_markers} took almost a full minute on the local machine.
\snakecase{create_markers_fast} reduced that time to roughly 33 seconds, while not affecting accuracy (Figure~\ref{fig:create-markers-speedup}).

\subsection{BIMPP}
\label{sec:evaluation-results-bimpp}

\subsubsection{sparse\_matrix\_template}

% ------------------------------------------------------------------------
%  NOTE (Claude): figure + numbers only, per the other results subsections.
%
%  Heaptrack, 6VYB, rank 1, both captures on the AMGX solver, local system,
%  before/after swapping bimpp's sparse_matrix_template storage from
%  std::vector<std::map<int,double>> to boost::container::flat_map
%  (see [[project_bimpp_memory]] in memory):
%      process peak heap:            5.84 GB -> 3.79 GB   (1.54x)
%      assemble_system_matrix peak:  3.81 GB -> 1.77 GB   (2.15x)
%  assemble_system_matrix is a SUBSET of the total, not additive -- the
%  figure draws it as an overlay, not a stacked sum.
%  Two allocation sites make up nearly all of the assembly figure:
%      entry storage      2.40G -> 1.02G   (RB-tree nodes -> flat vectors)
%      row-handle array   1.34G -> 672.21M (one alloc; 48B -> 24B per row)
%  i.e. the swap shrinks the per-entry storage AND the outer vector of row
%  handles, independently. Everything else under assembly is below 60MB.
%  Source: test1/profiles/ngpb.rank1.zst (std::map, captured 2026-09-01 17:38
%  against a reinstalled base bimpp) and the cook of the 13:43 capture
%  (flat_map). Raw captures via run_heaptrack.sh -r + scripts/cook_heaptrack.sh.
%
%  SAME CONFIG -- verified, not assumed. The container-independent mesh proxies
%  are identical digit for digit:
%      num_local_nodes    813.29M over 726488 calls   (both)
%      create_density_map      84882360 calls         (both)
%  and the entire 7.69M difference in total allocation count is accounted for
%  by the 7.71M difference at the assembly container site (agreeing to within
%  20k of 599M), i.e. the container is the only thing that changed.
%  This replaces an earlier pair (base.zst / opt.amgx.zst) that was NOT
%  same-config -- its mesh proxies differed by 13.5%, inflating the drop to
%  1.8x / 2.4x. Do not reuse those files.
%
%  The totals are heaptrack_print / Summary "peak heap memory consumption",
%  NOT the Top-Down "main" row: that tree has several roots (main, _start,
%  __clone3), merged-node peaks are documented as "not correct", and captures
%  can unwind to different depths and root the same allocation differently.
%  The assembly figures sum the peak consumers under
%  --filter-bt-function assemble_system_matrix, so they inherit that
%  merged-peak caveat; the totals do not.
%  Gotcha worth a sentence if you want it: flat_map was briefly catastrophic
%  when combined with a per-row .reserve(32) left over from an earlier change;
%  removing that reserve was part of this fix.
% ------------------------------------------------------------------------

Figure~\ref{fig:bimpp-memory-fix} shows the reduction of the memory consumption on rank 1 for 6VYB (test1).
As expected, the move from \snakecase{std::map} to \snakecase{boost::container::flat\_map} lead more than 2x reduction of the host memory usage during \snakecase{assemble\_system\_matrix}, which is the most memory intensive step of the entire NGPB pipeline.
The remaining memory utilization of around 2.02GB, coming mostly from the p8est mesh, is unchanged.
This change had no impact on accuracy, however it has slightly positive impact on the stage time of \snakecase{assemble\_system\_matrix}, going from an average of 22 seconds to 20.

\input{figures/bimpp_memory_fix.tex}

\subsubsection{bim3a\_solution\_with\_ghosts}

My optimization \snakecase{bim3a_solution_with_ghosts} reduced the time taken for 6YVB from 10 seconds to 60 milliseconds.
Being used at the end of every solver, and twice inside \snakecase{create_density_map}, this had an overall very positive effect on the total time for the full NGPB pipeline.
It shaved off roughly 30 seconds, with \snakecase{create_density_map} alone shrinking from taking 24 seconds to just 2.5 seconds.
This change also had no impact on accuracy.

\subsection{Baseline}


% TODO pick either perfil 2 to be the same more than twice of perfil 1
I got slightly different results for the two analytical tests than the original paper.
Some relative errors slightly lower for me than in the paper, while others slightly higher.
The order or magnitude stayed the same across the board.
The differences are shown in Table~\ref{tab:baseline-accuracy}.
I intent to use the lowest error in each case as the reference.
Interestingly, the triangulation-fix had no impact on these test.
The relative errors were exactly identical.

\input{figures/baseline_accuracy_table}

Although AMGX was set to a lower residual, it did not increase accuracy.
The relative error increased slightly to 

I accepted the improvements from the host-side optimization into the baseline.
The removed the necessity to swap between different BIMPP builds.
Also the focus of this thesis is on the CUDA optimizations, not on the host side.
% TODO make sure this isn't redundant

\subsection{Linear Equation Solver}
\label{sec:evaluation-results-lin-eq-solver}

Figure~\ref{fig:solver-bench} shows the relative speedup achieved by AMGX for all molecules.
Despite being set to a much smaller residual, the default configuration for AMGX manages to beat LIS at every sample size on the local system.
The tuned AMGX configuration runs faster than the default one, also at every sample size.

One interesting observation I made is that despite having more than 4x the atoms, 1VSZ's solve was a bit quicker than 6VYB's across the board.
Upon inspection the number of quadrants and nodes for each molecule, 6VYB's grid had 28008707 nodes and 28294120 quadrants, while 1VSZ had 15879325 nodes and 16073576 quadrants.
The number nodes equate to the dimension of the solution vector of the linear equation, which explains why 1VSZ's solve time is quicker.
This implies that the number of atoms is not the only factor that predicts the size of the linear system.
I suspect the geometric complexity plays a role here, since 6VYB's shape is quite irregular, and 1VSZ's shape is icosahedral.
Figure~\ref{fig:molecule-shapes} shows the shapes of both molecules.

\begin{figure}[!htb]
\centering
\begin{subfigure}[b]{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/shape_6vyb.jpeg}
    \caption{6VYB}
    \label{fig:shape-6vyb}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.47\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/shape_1vsz.jpeg}
    \caption{6CGV}
    \label{fig:shape-1vsz}
\end{subfigure}
\caption[Molecular shapes of 6VYB and 1VSZ]{Cartoon renderings of the biological assemblies, colored by chain. \textbf{Left:} 6VYB's irregular, elongated spike trimer. \textbf{Right:} the icosahedral capsid of 1VSZ, shown via its current PDB entry 6CGV, since 1VSZ itself is obsolete. Renderings from the RCSB PDB.}
\label{fig:molecule-shapes}
\end{figure}

The number of iterations are shown on top of the columns, as well as in the Table~\ref{tab:solver-bench}. 
Even though the default ``CG-Jacobi-Block'' needs consistently a lot more iterations than both LIS and the tuned configuration (in cases of 6VYB and 1VSZ 10x as many than tuned), it was still much quicker than LIS, and only marginally slower than the tuned configuration.
This demonstrates that having very cheap iterations can still make up for the having to do a lot of them.

\input{figures/solver_bench.tex}
\input{figures/solver_bench_table.tex}

I then compared the AMGX solve time for 6VYB for four different topologies: the local system, a one cluster node with a single A100 GPU, one with dual A100 GPUs and two nodes with one A100 each, shown in Figure~\ref{fig:amgx_scaling_6vyb}.
I omitted the runtime for LIS on the A100 cluster node, since it ran much slower due to their old CPUs.
The CPU of the H200 is much better, making it a much fairer comparison.
The goal was to see how well AMGX scales across multiple nodes, and GPUs.
To compare the single cluster node to the local system, I also included 4-rank runs on the cluster to match the local runs.

Comparing the local system to a single cluster node with one A100 GPU launched with 4 ranks, the actual solve time decreased by over 2x.
This is again, due to the weak cluster CPU, which slows down the host portion of the linear solve.
Increasing the number of ranks to 16 reduces the negative impact from the CPU, while the GPU solve time stays basically unchanged.

The figure also shows that AMGX scales much better with multiple GPUs per node, instead of across multiple nodes.
The tuned configuration performed significantly worse on two nodes than it did on a single node.
This is very likely due to communication overhead, which is much lower an a single node with multiple GPUs.
There the tuned AMGX performed much better than the single GPU node.
Surprisingly, the default configuration did not suffer as much, although it was barely any faster than one single GPU node.
This suggests that AMG preconditioning is more communication-intensive than even a multitude of cheap iterations.

\input{figures/amgx_scaling_6vyb}
\input{figures/amgx_scaling_6vyb_table}

\subsection{FMM Parameters}
\label{sec:evaluation-results-fmm-parameters}

The FMM has multiple parameters, which I needed to balance against each other to find the optimum, those being the order of the multipole expansion $P$, the MAC parameter $\theta$ and the number of particles inside one leaf $n_{leaf}$.
I made four sweeps of $P$ and $\theta$ for values between 6 to 10 and 0.2 to 0.8 for 6VYB (test1) and 1VSZ (test2) on my local machine.

FMM was slower than the naive kernel for 1CCM, no matter which configuration I used, which is why I did not consider it for the parameter choice.
This molecule consists of only 642 atoms, at which point the overhead of FMM is simply too expensive.
I've plotted their speed against their relative error compared to the naive $O(N^2)$ GPU sum over the same inputs, taken from an average of 3 runs,
which can be seen in Figure~\ref{fig:fmm_presplit_6vyb} and \ref{fig:fmm_presplit_1vsz}.
In those figures and in every later sweep figure, color is the multipole order $P$ and the points along each line are the MAC parameter $\theta$, rising from $0.2$ at the accurate end to $0.8$.
Grey marks the Pareto front over the whole sweep, and the dashed vertical line the naive path's own time.

% ------------------------------------------------------------------------
%
%  Regenerated with (from the repository root):
%    M='the local system (Table~\ref{tab:test-systems})'
%    K=1.246641e-10
%    python3 scripts/plot_fmm_sweep.py test1 --csv test1/fmm_sweep_6VYB_presplit.csv \
%        --tag fmm_presplit_6vyb --molecule 6VYB --natoms 44495 \
%        --kirkwood-err $K --machine "$M"
%    python3 scripts/plot_fmm_sweep.py test2 --csv test2/fmm_sweep.csv \
%        --tag fmm_presplit_1vsz --molecule 1VSZ --natoms 14299 \
%        --kirkwood-err $K --machine "$M"
%    python3 scripts/plot_fmm_sweep.py test1 --csv test1/ptheta.csv \
%        --tag fmm_ptheta_6vyb --molecule 6VYB --natoms 44495 \
%        --kirkwood-err $K --machine "$M"
%    python3 scripts/plot_fmm_sweep.py test2 --csv test2/confirm_16_512.csv \
%        --tag fmm_ptheta_1vsz --molecule 1VSZ --natoms 14299 \
%        --kirkwood-err $K --machine "$M"
%  Add --stock-csv test<N>/bench_runs.csv to print the reproducibility numbers
% ------------------------------------------------------------------------

\input{figures/fmm_presplit_6vyb.tex}
\input{figures/fmm_presplit_1vsz.tex}

Since it was my goal to achieve a relative error of the polarization energy of 1.5e-10 or lower, two configurations for test1 seem to be reaching that goal while being the fastest: $P = 9$, $\theta = 0.3$, $n_{leaf} = 64$ and $P = 10$, $\theta = 0.4$, $n_{leaf} = 256$.
Profiling both configurations using \snakecase{ncu} revealed that each configurations are quick for very different reasons.
The \snakecase{l2p_p2p_kernel} ran much quicker with $P = 9$, $\theta = 0.3$, $n_{leaf} = 64$ while \snakecase{m2l_accumulate_kernel} was much quicker with $P = 10$, $\theta = 0.4$, $n_{leaf} = 256$.
The underlying reason is that the source tree containing the atoms and the target tree containing the surface points have wildly different sizes.
At a grid of scale of 2, the surface points far outnumber the number of atoms in the molecule, and they are also much denser.
Clustering both trees with the same leaf count causes wildly different geometric leaf sizes.
M2L cost tracks with the total box-pair computed by the DTT, which were dominated by the number of box nodes of the much denser target tree.
Increasing the number of particles per leaf reduced that number, making M2L faster.
However, raising the number of particles per leaf also increased the radii used in the MAC test (see Figure~\ref{fig:fmm-mac}).
This meant it would fail more often, inflating the number of direct P2P interactions.
Considering two best settings sat a $n_{leaf}$ of 64 and 256, but not 128 showed that this relationship was a U-shaped curve, with no optimum in between.
The solution was to split $n_{leaf}$ into source tree leaf counts $n_{leaf,src}$ and target tree leaf counts $n_{leaf,tgt}$.
This allowed me to reduce $n_{leaf,src}$ while increasing $n_{leaf,tgt}$.
Setting them to 64 and 256 respectively at $P = 10$, $\theta = 0.4$ slashed the time in half!

Next I tried to find the optima for both values.
As seen in Figures~\ref{fig:leaf_sweep_6vyb} and \ref{fig:leaf_sweep_1vsz} the optimal values of $n_{leaf,src}$ and $n_{leaf,tgt}$ diverged into different directions.
It also shows that they differ for both molecules, which is probably due to their different shapes.
I initially assumed that the ideal counts would exactly where the geometric leaf sizes of both tree would be equal, but further testing demonstrated that the optima shifted with different $P$ and $\theta$.
This can be explained by the cost of P2P and M2L not being equal.
The balance shifts towards P2P when the cost of M2L increases through rasing of $P$.
I demonstrated this effect for 6VYB in the left diagram of the Figure~\ref{fig:leaf_optimum_shift_6vyb}, where the optimal $n_{leaf,src}$ rises monotonically with the order of $P$ (4, 4, 8, 8, 16, 16 for $P$ 6..11).
Meanwhile $\theta$ has a much weaker secondary effect, flattening the the curve as it increases but not relocating the optimum.
Over $\theta = 0.3$ to $0.6$ the optimum does not move at all.

\input{figures/leaf_sweep_6vyb.tex}
\input{figures/leaf_sweep_1vsz.tex}

\input{figures/leaf_optimum_shift_6vyb.tex}
\input{figures/leaf_optimum_shift_6vyb_table.tex}

This also means that this balances is shifted once again on the cluster, where the cost of P2P is greatly reduced, thanks to higher FP64 performance.
The left diagrams of Figures~\ref{fig:platform_shift_1vsz} and \ref{fig:platform_shift_6vyb} both show this effect though an increase in optimal $n_{leaf,src}$, which increases the number of P2P calculations through the two mechanism that explained above.
On top of it all, the number of ranks also influence the balance.
Each rank holds an exact copy of the source tree, but their target tree only is built from the particles of the domain region owned by the rank.
Even though regions are not equally populated, this still roughly equates to the number of particles being divided by the number of ranks, acting like a multiplier on the $n_{leaf,tgt}$.
This can be clearly seen in the right diagram of Figure~\ref{fig:platform_shift_6vyb}, showing a decrease in the optimal $n_{leaf,tgt}$.
Though the same cannot be objectively said about 1VSZ, it is clear from the curve that the A100-16 rank curve is almost flat between 512 and 1024, while the A100-4 rank curve has much more clear minima.

In terms of accuracy: the rank 4 results from the A100 and the RTX 3080 are within a $10^{-12}$ relative error.
An increase in ranks had the tendency to decrease the median error slightly, but this was only observed across 62\%-67\% of all runs.
I will refrain from calling this a general trend, but rather fluctuations attributed to the observations made in \ref{sec:evaluation-experimental-setup-mpi-ranks}.

\input{figures/platform_shift_6vyb.tex}
\input{figures/platform_shift_6vyb_table.tex}
\input{figures/platform_shift_1vsz.tex}

Despite the multitude of variables, the general direction for searching optima is clear, and considering the accuracy target, the mission now became a search for the fastest configuration that still satisfies it.
I converged towards a local optimum through successive sweeps of the best configuration.

One discovery I made while searching was that accuracy did not always monotonically increase with $P$.
As an example: for 1VSZ and $\theta = 0.3$, $n_{leaf,src} = 16$, $n_{leaf,tgt} = 512$ the relative error went 8.0e-09, 4.1e-11, 6.8e-10, 1.2e-12, 7.7e-12, 1.6e-12, 3.0e-13 for $P = 6..12$.
The relative error became worse going from $P = 7$ to $P = 8$.
This necessitated the sweep across all $P$ every time I converged on a new optimum.
Nevertheless it is not unexpected.
The Cheng-Greengard-Rokhlin give an upper bound on a multipole converted to a local expansion with Theorem 2.4 of \cite{cheng1999fastadaptive}, and it roughly decays with $(\text{seperation ratio})^{(P+1)}$ scaled by the sum of the absolute charges.
However three things made decide against striving towards it.
Since the atoms carry both negative and positive charges, their net charge may be much lower than the sum of their absolutes.
This is something Salmon \& Warren also state in \cite{salmon1994skeletons} \textit{``...the monopole contribution of a cell may be vanishingly small due to charge cancellation...''}.
Second, this boundary makes no judgement on speed, just accuracy, and Salmon \& Warren have also shown that a few percent of accuracy may cost about 30 times as much computation.
Lastly, it does not mean that the error for a singular result will be exactly on that boundary, or that it will also decrease monotonically with $P$.
At this point I need to clarify that Salmon \& Warren are not opposed to theoretical bounds.
% TODO decide whether to include the θ/(2−θ) rate

For my local machine the measured best configurations for 6VYB (test1) and 1VSZ (test2) can be seen in Table~\ref{tab:fmm_ptheta_6vyb-pareto} and \ref{tab:fmm_ptheta_1vsz-pareto}.
For the stated accuracy goal of a potential energy relative error of less than 1.5e-10, the most efficient config for 6VYB was $P=11$, $\theta=0.4$, $n_{leaf,src} = 16$ $n_{leaf,tgt} = 1024$.
For 1VSZ the most efficient config was $P=7$, $\theta=0.3$, $n_{leaf,src} = 16$ $n_{leaf,tgt} = 512$.

\input{figures/fmm_ptheta_6vyb.tex}
\input{figures/fmm_ptheta_6vyb_table.tex}
\input{figures/fmm_ptheta_1vsz.tex}
\input{figures/fmm_ptheta_1vsz_table.tex}


\subsection{Energy Calculation}
\label{sec:evaluation-results-energy-calculation}

The computation time for the energy calculations on the local machine for 1CCM, 1VSZ and 6VYB can be seen in Figure~\ref{fig:energy-methods}, as well as Table~\ref{tab:energy-methods}.
I used the best configuration I had found for each molecule.
As already hinted at in \ref{sec:evaluation-results-fmm-parameters}, FMM no configuration managed to beat the naive kernel for 1CCM.
This shows that despite having a time complexity of \(\mathcal{O}(N)\), FMM has a clear offset cost, making it slower than a naive approach for smaller problems, which are not compute bound.
Aside from this, FMM managed to bring a significant speedup over the naive kernel, let alone the CPU path.
The naive kernel also brought a massive speedup over the CPU path as well.

% TODO check the FMM configurations, something doesn't seem right here
\input{figures/energy_methods.tex}
\input{figures/energy_methods_table.tex}

I also ran 6VYB, 1VSZ, as well as H1N1 on the cluster, which can be seen in Figure~\ref{fig:energy-methods-cluster} and Table~\ref{tab:energy-methods-cluster}.
6VYB and 1VSZ were run on a single A100 node, while H1N1 was run on two H200 nodes, that have two GPUs each.
Note that each CPU run was taken from the cluster CPU. %TODO maybe I should just replace those with the local CPU times
Now running on a workstation GPU, the naive kernel are not FP64 bound anymore, and the gap between it and FMM, which existed on the local machine, basically disappears.
1VSZ is now slightly faster using the naive kernels, while 6VYB is slightly faster using FMM, and this was before the leaf-count split, still running on the old $P = 10, \theta = 0.4, n_{leaf} 256$ configuration.
Nevertheless, the gap reappears on H1N1, where FMM once again shows a massive speedup over the naive kernel.
The three results really demonstrate how FMM pays an upfront cost, that gets pays off massively once the problem becomes compute-bound.
I have to point out that I didn't manage to capture the CPU time for H1N1, which ran out of time within its 12-hour slot, of which the other stages only used 49 minutes.
Even a conservative estimate of 11 hours would mean the naive kernels --- having taken 52 minutes --- are at least 12.5x faster, which really showcases how well GPUs can boost performance through sheer strength.
Conversely, I had to compare accuracy of the FMM with the naive kernel.
Given that the naive kernel is the exact math, not an approximation, this should be perfectly fine comparison.

\input{figures/energy_methods_cluster}
\input{figures/energy_methods_cluster_table}

Lastly I want to compare how FMM and the naive kernels scale across the topologies using Figure~\ref{fig:energy_scaling_6vyb} and Table~\ref{tab:energy_scaling_6vyb}.
Going from the RTX 3080 to its bigger sibling, the A100, we see a huge performance increase for the naive kernel, really accentuating the lack of FP64 units on the former.
The improvement for FMM is much smaller, but it also gives an idea of how much it reduces the compute intensity of the N-Body calculation.
% TODO these are wall clocks for the full stage, that sucks! The workstation CPU is way slower.
Unlike AMGX, neither FMM nor the naive kernels need to communicate during the computation.
Therefore there the two-node setup scales normally. 
In case of FMM, it is scales even better than the one dual GPU node.

\input{figures/energy_scaling_6vyb}
\input{figures/energy_scaling_6vyb_table}
% ------------------------------------------------------------------------
%
%  Regenerated with (from the repository root):
%    python3 scripts/plot_energy_methods.py test0 test1 test2 \
%        --accuracy 1.246641e-10 \
%        --machine 'the local system (Table~\ref{tab:test-systems})' \
%        --note '1CCM=swept before the source/target leaf split, so both trees used one leaf size'
%
% ------------------------------------------------------------------------
%
%  The same three methods, but every bar measured on the cluster, which is the
%  only place 1VSZ, 6VYB and H1N1 can all three be run. Regenerated with:
%    python3 scripts/plot_energy_methods.py test6/leafsweep_1VSZ test5/leafsweep_6VYB \
%        --tag energy_methods_cluster \
%        --extra 'H1N1:cpu=test7/h1n1-0-1884559.out' \
%        --extra 'H1N1:naive=test7/h1n1-1-1884560.out' \
%        --extra 'H1N1:fmm=test7/h1n1-2n-1883766.out' \
%        --extra-fmm 'H1N1=$P = 10$, $\theta = 0.4$, $n_{leaf} = 256$' \
%        --dnf-limit 12 \
%        --machine 'an A100 node of the TU Berlin HPC (Table~\ref{tab:test-systems})' \
%        --machine-override 'H1N1=four H200 GPUs of the TU Berlin HPC (Table~\ref{tab:test-systems})' \
%        --note 'H1N1=the FMM run was spread over two nodes and the naive run over one, but both produced the same surface and potential, their flux charges agreeing to $2.2\times10^{-12}$'


% ------------------------------------------------------------------------
%
%  Regenerated with (from the repository root):
%    python3 scripts/plot_energy_scaling.py \
%        "RTX 3080|1 GPU|4 ranks=test1/bench_runs.csv" \
%        "A100|1 GPU|4 ranks=test5/rank4_6VYB/bench_runs.csv" \
%        "A100|1 GPU|16 ranks=test5/scaling_6VYB_1gpu/scaling_1gpu.csv" \
%        "A100|2 GPUs|1 node=test5/scaling_6VYB_2gpu/scaling_2gpu.csv" \
%        "A100|2 GPUs|2 nodes=test5/scaling_6VYB_2node/scaling_2node.csv" \
%        --tag energy_scaling_6vyb
%
%  Same five columns and the same x axis as figures/amgx_scaling_6vyb, so the
%  two can be read against each other.
%
%  NOTE (Claude): the numbers, so you do not have to dig through the CSVs.
%  Delete this block once the paragraph is written.
%
%  t_energy medians (s), pooled over both linear solvers -- the solve finishes
%  before the energy stage starts, so it cannot reach it:
%
%                        RTX3080/4r   A100/4r   A100/16r   2 GPU   2 node
%      FMM                   3.30       ----       1.49     1.27     1.19
%      naive O(NM)          24.67       2.73       1.81     1.34     1.29
%      CPU                    422       1015        545      540      482
%      pipeline                96        208         89       85       85
%
%  1. It scales monotonically and does NOT invert across nodes -- the opposite
%     of the AMG configuration in sec:evaluation-results-lin-eq-solver, which
%     drops to 0.61x on two nodes. Two nodes is the FASTEST column in both GPU
%     rows: naive 1.81 -> 1.34 -> 1.29 (1.40x), FMM 1.49 -> 1.27 -> 1.19
%     (1.25x). The stage is per-rank work with a single reduction at the end,
%     so the 6.10 us inter-node latency in tab:mpi-latency-table never bites.
%     The gains are sub-linear because the host-side collection phase inside
%     t_energy does not shrink with the GPU count.
%  4. t_energy IS contaminated by the CPU -- it is one TOC around the whole
%     stage, and phase 1 (collect_energy_inputs_fast, pb_class.cpp:3732, the
%     host-side border_quad loop that gathers the flux points and surface
%     triangles) is inside it and does not shrink with GPU count. But it does
%     NOT invalidate the three A100 columns: they all run np=16 on the same
%     mesh and the same partition, so phase 1 is the SAME constant in all
%     three. Every A100-to-A100 difference in the figure is therefore already
%     pure GPU time, and every A100-to-A100 ratio is a LOWER BOUND on the true
%     GPU ratio -- subtracting a common positive offset from both sides can
%     only increase it. Writing P for that offset, the 1 GPU -> 2 nodes ratio:
%
%         P [s]      0      0.2     0.4     0.6
%         naive    1.40    1.47    1.58    1.75
%         FMM      1.25    1.30    1.38    1.51
%
%     So the figure understates the GPU scaling; it never overstates it.
%     What phase 1 DOES break is the two 4-rank columns against the 16-rank
%     ones, where the rank count and the host both change. Do not read those
%     two steps as GPU speedups.
%
%     P has not been measured on the cluster -- bench_sweep.py kept only the
%     energy_method=0 REF log per folder, so there is no finer stdout to mine,
%     and no cluster run was made with energy_dump set. It IS measurable
%     locally right now, both methods at once, since the dump and the binary
%     already exist:
%       mpirun -n 4 src/fmm_replay test1/fmm_inputs \
%           --mac 0.4 --order 11 --leaf 16 --tleaf 1024 --repeats 3 \
%           --csv /tmp/phase2_6vyb.csv
%     phase 1 = t_energy - replay total, and the FMM and naive rows must give
%     the same P. That pins the local 4-rank column only.
%
%  5. THE TWO CLUSTER FIGURES DISAGREE ON 6VYB's FMM TIME, and both are right.
%     They come from different sweeps, which ran different leaf pairs:
%
%       Fig energy-methods-cluster   0.3 / p9  / 16/1024   1.440 s
%       Fig energy-scaling-6vyb      0.4 / p11 / 32/1024   1.486 s   (1 GPU)
%
%     Same molecule, same 16 ranks, same A100 -- 3% apart purely because of the
%     configuration. Each table states its own, so neither figure is wrong, but
%     a reader flipping between them sees two "FMM on an A100" numbers. Worth
%     one sentence, or pick one sweep for both figures and regenerate.
%
%     The leaf pairs are deliberate, not stale: sweep "s" hardcodes them
%     (bench_sweep.py, the "s" branch) and the table above it records the full
%     4x6 grid they came from. They are the 16-RANK CLUSTER optima, which are
%     NOT the 4-rank local ones -- the target points are distributed across
%     ranks while the atom tree is replicated, so the rank count moves the
%     optimum. The local 8/1024 pair measures 2.9% slower on the cluster.
%     The scaling folders' own options.prm is misleading here: it says
%     fmm_multipole_order = 10 and carries no leaf keys at all. It is inert --
%     sweep "s" sets all four fmm_* keys per row, so those prm values never
%     applied. The CSV columns are the authoritative record of what ran.
%
%  Two caveats the figure and table show rather than hide:
%    - No FMM run exists at 4 ranks on the A100, so that bar is drawn as a
%      marked gap. Nothing was interpolated.
%    - The local FMM bar is n_leaf,src=16 where the cluster used 32 (same
%      theta=0.4, P=11, n_leaf,tgt=1024). Hence the config column in the table.
%
%  Both panels are logarithmic: the naive panel spans 19x on its own, which is
%  the 3080's FP64 rate rather than the topology, and on a linear axis the four
%  A100 bars collapse into stubs. figures/energy_methods is log for the same
%  reason.
% ------------------------------------------------------------------------